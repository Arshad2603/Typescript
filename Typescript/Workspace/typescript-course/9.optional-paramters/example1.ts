function additionNumbers(a:number, b:number, c?: number) {
    return a + b;
}
var result  = additionNumbers(2, 3); //c is an optional parameter
console.log(result);
//c is an optional parameter using ? after the parameter name